"""demos/ — the demo kit (SPEC A.R9). Offline, key-free, deterministic.

Run from the repo root, or via the Makefile:

    make demo-break     # demos/demo_break.py
    make demo-al        # demos/demo_al.py

This file exists so `tests/test_demo.py` can import the modules directly
(`from demos import demo_break`) as well as drive them as subprocesses.
"""
